# =============================================================================
# DNS Validation Configuration
# =============================================================================
#
# Configure the expected DNS settings for your services.
# Add these variables to your .env file and customize as needed.
#

# -----------------------------------------------------------------------------
# Default Service Configuration
# -----------------------------------------------------------------------------
# Nameservers esperados (separados por coma, sin espacios)
DNS_NAMESERVERS=ns1.revisionalpha.com,ns2.revisionalpha.com

# IPs válidas (separadas por coma, sin espacios)
DNS_VALID_IPS=51.83.76.40,51.195.217.63,66.70.189.5

# Dominio SPF a verificar en registros TXT
DNS_SPF_INCLUDE=spf.revisionalpha.com

# -----------------------------------------------------------------------------
# Additional Services (Optional - Example)
# -----------------------------------------------------------------------------
# Puedes agregar servicios específicos si necesitas validaciones diferentes

# VPS Service
# DNS_VPS_NAMESERVERS=ns1.example.com,ns2.example.com
# DNS_VPS_VALID_IPS=192.0.2.1,192.0.2.2
# DNS_VPS_SPF_INCLUDE=spf.example.com

# Mailer Service
# DNS_MAILER_NAMESERVERS=ns1.mailserver.com,ns2.mailserver.com
# DNS_MAILER_VALID_IPS=198.51.100.1,198.51.100.2
# DNS_MAILER_SPF_INCLUDE=spf.mailserver.com

# -----------------------------------------------------------------------------
# Default Service Name
# -----------------------------------------------------------------------------
# El servicio por defecto a usar si no se especifica ninguno
# Usa 'default' para las variables genéricas (DNS_NAMESERVERS, etc.)
# O especifica un servicio personalizado (vps, mailer, etc.)
DNS_DEFAULT_SERVICE=default

